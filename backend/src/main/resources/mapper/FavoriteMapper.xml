<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devtoolmp.mapper.FavoriteMapper">

    <resultMap id="BaseResultMap" type="com.devtoolmp.entity.Favorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="tool_id" property="toolId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE id = #{id}
    </select>

    <select id="findByUserIdAndToolId" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE user_id = #{userId} AND tool_id = #{toolId}
    </select>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE user_id = #{userId}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.devtoolmp.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorites (user_id, tool_id, created_at)
        VALUES (#{userId}, #{toolId}, #{createdAt})
    </insert>

    <delete id="deleteById">
        DELETE FROM favorites WHERE id = #{id}
    </delete>

    <delete id="deleteByUserIdAndToolId">
        DELETE FROM favorites WHERE user_id = #{userId} AND tool_id = #{toolId}
    </delete>

    <select id="existsByUserIdAndToolId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM favorites WHERE user_id = #{userId} AND tool_id = #{toolId}
    </select>

</mapper>
