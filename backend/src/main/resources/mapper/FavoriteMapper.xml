<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devtoolmp.mapper.FavoriteMapper">

    <resultMap id="BaseResultMap" type="com.devtoolmp.entity.Favorite">
        <id column="id" property="id"/>
        <result column="client_identifier" property="clientIdentifier"/>
        <result column="tool_id" property="toolId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE id = #{id}
    </select>

    <select id="findByClientIdentifierAndToolId" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE client_identifier = #{clientIdentifier} AND tool_id = #{toolId}
    </select>

    <select id="findByClientIdentifier" resultMap="BaseResultMap">
        SELECT * FROM favorites WHERE client_identifier = #{clientIdentifier}
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByClientIdentifier" resultType="int">
        SELECT COUNT(*) FROM favorites WHERE client_identifier = #{clientIdentifier}
    </select>

    <insert id="insert" parameterType="com.devtoolmp.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorites (client_identifier, tool_id, created_at)
        VALUES (#{clientIdentifier}, #{toolId}, #{createdAt})
    </insert>

    <delete id="deleteById">
        DELETE FROM favorites WHERE id = #{id}
    </delete>

    <delete id="deleteByClientIdentifierAndToolId">
        DELETE FROM favorites WHERE client_identifier = #{clientIdentifier} AND tool_id = #{toolId}
    </delete>

    <select id="existsByClientIdentifierAndToolId" resultType="boolean">
        SELECT COUNT(*) > 0 FROM favorites WHERE client_identifier = #{clientIdentifier} AND tool_id = #{toolId}
    </select>

</mapper>
